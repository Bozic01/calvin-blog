{% extends 'base_admin.html.twig' %}

{% block title %}PostCategory{% endblock %}

{% block body %}
    <h1>PostCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post_category.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ post_category.title }}</td>
            </tr>
        </tbody>
    </table>

    <a
       href="{{ path('admin_post_category_edit', {'id': post_category.id}) }}"
       class="btn btn-dark"
    >
        Edit
    </a>

    <a href="{{ path('admin_post_category_index') }}" class="btn btn-secondary">
        Back
    </a>

    {{ include('admin/post_category/_delete_form.html.twig') }}
{% endblock %}
